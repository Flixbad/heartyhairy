{% extends 'base.html.twig' %}

{% block body %}
<h1 class="text-3xl font-bold text-yellow-300 text-center mb-8">💳 Choisissez votre mode de paiement</h1>

<div class="max-w-lg mx-auto bg-white/90 p-6 rounded-xl shadow">
  {{ form_start(form) }}
    {{ form_row(form.payment) }}
    <div class="mt-6 text-center">
      <button type="submit"
              class="bg-yellow-500 text-black px-6 py-2 rounded-full font-bold hover:bg-yellow-400 transition">
        Continuer
      </button>
    </div>
  {{ form_end(form) }}
</div>
{% endblock %}
